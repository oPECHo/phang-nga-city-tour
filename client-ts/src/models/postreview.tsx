export default interface Postreview {
    data: {
        id_tour: string;
        name_tour: string;
        author: string;
        user: string;
        score: number;
        comment: string;
        Date: string
    }
}