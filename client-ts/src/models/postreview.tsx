export default interface Postreview {
    data: {
        tour_id: number;
        tour_name: string;
        author: string;
        user: string;
        score: number;
        comment: string;
        Date: string
    }
}